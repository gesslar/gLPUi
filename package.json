{
  "name": "oxidus-ui",
  "version": "1.8.0",
  "description": "A Mudlet GUI for the Oxidus mudlib.",
  "scripts": {
    "build": "npx muddy",
    "watch": "npx muddy -w",
    "major": "npx muddy version major --package",
    "minor": "npx muddy version minor --package",
    "patch": "npx muddy version patch --package",
    "pr": "gt submit -p --ai"
  },
  "license": "0BSD",
  "devDependencies": {
    "@gesslar/muddy": "^3.2.2"
  }
}
